# TODO

- Make sure components that take in array props have a default of [] if they don't have a source
- Add boolean[] array prop
- Fix camera component
- Fix Field2d duplicate robot issue
- ✅ Make width/height of dashboard really large in edit mode. Show bounding box in edit mode which defaults to full width and height of screen. Make these dashboard settings.
- Store latest NT4 connection address in local storage and uses that on refreshes
